{% extends 'base.html.twig' %}

{% block title %}Temat{% endblock %}

{% block body %}
    <h2>Temat</h2>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ temat.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ temat.name }}</td>
            </tr>
            <tr>
                <th>SubFamily</th>
                <td>{{ temat.subFamily }}</td>
            </tr>
            <tr>
                <th>SpeciesCount</th>
                <td>{{ temat.speciesCount }}</td>
            </tr>
            <tr>
                <th>FunFact</th>
                <td>{{ temat.funFact }}</td>
            </tr>
            <tr>
                <th>IsPublished</th>
                <td>{{ temat.isPublished ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_index') }}">back to list</a>

    <a href="{{ path('admin_edit', {'name': temat.name}) }}">edit</a>

    {{ include('admin/temat/_delete_form.html.twig') }}
{% endblock %}
